import { ref } from 'vue'
import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', () => {
  // Add app-wide state here as needed
  // Example: user settings, theme, etc.

  return {
    // Export reactive state and actions here
  }
})
